// Used by the react-native Jest preset to transform react-native's own
// Flow-typed JavaScript (the SDK's TypeScript is compiled by ts-jest).
module.exports = {
  presets: ['module:@react-native/babel-preset'],
};
