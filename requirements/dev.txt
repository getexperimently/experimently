-r base.txt

# Debugging extras not needed in CI or production.
# (ruff, mypy and mkdocs are already pinned in backend/requirements.txt;
# ruff replaced black, isort and flake8)
ipython==8.15.0
debugpy==1.6.7
