# Experimently Requirements
#
# backend/requirements.txt is the single source of truth for Python
# dependencies (it is what the Dockerfile, the CI workflows and Dependabot
# use).  This file exists so that `pip install -r requirements.txt` from the
# repository root keeps working.
#
# The requirements/ directory holds thin profile files (dev/test/prod) that
# layer a few extras on top of backend/requirements.txt.

-r backend/requirements.txt
