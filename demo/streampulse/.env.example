# Copy to .env.local (the seed script writes NEXT_PUBLIC_EXPERIMENTLY_API_KEY there for you).

# Experimently backend (FastAPI). The SDK appends /api/v1 itself.
NEXT_PUBLIC_EXPERIMENTLY_API_URL=http://localhost:8000

# Plaintext API key for the `streampulse-app` key owned by admin@demo.com.
# Created by backend/scripts/seed_streampulse.py; also written to demo/streampulse/.api_key.
NEXT_PUBLIC_EXPERIMENTLY_API_KEY=

# Experimently dashboard (Next.js frontend) — used for the links in the Experimently panel.
NEXT_PUBLIC_EXPERIMENTLY_DASHBOARD_URL=http://localhost:3100
