import React from 'react';
import StreamPulseApp from '@/components/StreamPulseApp';

/** The whole demo is one page; see components/StreamPulseApp.tsx. */
export default function IndexPage() {
  return <StreamPulseApp />;
}
