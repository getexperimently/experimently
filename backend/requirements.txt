# API Framework
fastapi==0.141.1
uvicorn==0.53.0
websockets==15.0.1     # uvicorn WebSocket support (/api/v1/ws/...); the image has no wsproto
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0
python-multipart==0.0.32
PyJWT==2.14.0
bcrypt==5.0.0

# Database & ORM
SQLAlchemy==2.0.54
alembic==1.20.0
psycopg2-binary==2.9.13
asyncpg==0.31.0
aioredis==2.0.1
redis==8.1.0
boto3==1.43.99
boto3-stubs==1.43.99
mypy-boto3-dynamodb==1.43.64
mypy-boto3-ssm==1.43.53

# Scheduling & Background Tasks
APScheduler==3.11.3
pytz==2026.3.post1

# AWS
aws-cdk-lib==2.270.0
constructs==10.8.1                 # pinned here too, and at the same version,
                                   # because this file is what CI and the venv
                                   # install and therefore what `cdk synth`
                                   # actually runs against; see
                                   # infrastructure/cdk/requirements.txt
aws-cdk.asset-awscli-v1==2.2.292   # pinned exactly (==) by aws-cdk-lib 2.270.0;
                                   # a grouped Dependabot update moved this to
                                   # 2.2.294 while also moving aws-cdk-lib to
                                   # 2.270.0, which forbids it -- pip then could
                                   # not resolve the file at all
aws-cdk.asset-kubectl-v20==2.1.4
aws-cdk.asset-node-proxy-agent-v6==2.1.3
aws-cdk.cloud-assembly-schema==54.24.0   # aws-cdk-lib 2.270.0 needs >=54.11,<55
aws-lambda-powertools==2.43.1
aws-xray-sdk==2.15.0
watchtower==3.4.0  # CloudWatch logging

# Testing
pytest==7.4.4
pytest-asyncio==0.23.8
pytest-cov==6.3.0
pytest-mock==3.15.1
httpx==0.28.1
slack-sdk==3.44.1
sendgrid==6.12.5
faker==40.39.0
factory-boy==3.3.3
freezegun==1.5.5
moto==4.2.14
pyparsing==3.3.3       # required by moto's glue backend (mock_glue), not declared by moto 4.2
python-jose[cryptography]==3.5.0  # required by moto's cognitoidp backend (mock_cognitoidp); app auth uses PyJWT
jsonschema==4.26.0
locust==2.39.1   # 2.40+ requires pytest>=8.3; we pin pytest 7.4.
                 # A grouped Dependabot update moved this to 2.46.6,
                 # straight past the constraint on this line, and pip
                 # then could not resolve the file. Raising it needs
                 # the pytest 7 -> 9 migration first (#225).

# Statistical Analysis
numpy==1.26.4   # scipy 1.17 needs numpy>=1.26; 2.x is a separate major bump
scipy==1.17.1
pandas==2.3.3   # statsmodels 0.15 needs pandas>=2.2
statsmodels==0.15.0

# Utilities
python-dateutil==2.9.0.post0
PyYAML==6.0.3
Jinja2==3.1.6
tenacity==9.1.4
structlog==23.3.0
orjson==3.12.0
ujson==6.0.0
semver==3.1.0          # core/rules_engine.py semantic-version operators
loguru==0.7.3          # api/deps.py
requests==2.34.2       # tests only: tests/smoke/test_production.py and
                       # tests/realistic/*.  Deliberately NOT in
                       # requirements/runtime.txt -- no backend/app, backend/lambda
                       # or backend/scripts module imports it; the one runtime
                       # importer is a module service, so the full image gets it
                       # from modules/requirements.txt.
defusedxml==0.7.1      # a modules runtime pin (modules/requirements.txt), kept
                       # here too because this file is what every test job and
                       # every developer venv installs, and the modules tree
                       # imports it UNGUARDED (sso_service.py, deliberately: the
                       # XXE-safe parser has no safe fallback).  Without it,
                       # modules.register(hooks) raises on a full checkout and
                       # conftest exits 3 with zero tests collected.
                       # Deliberately NOT in requirements/runtime.txt -- nothing
                       # under backend/ imports it.
cryptography==50.0.1   # core/phi_encryption.py (Fernet)
mock==5.2.0            # tests/unit/middleware/test_error_middleware.py

# Monitoring & Tracing
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp==1.44.0
prometheus-client==0.26.0

# Development
ruff==0.16.8          # lint + format; replaced black, isort and flake8
mypy==1.20.2
pre-commit==3.8.0

# Documentation
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocstrings==0.30.1
mkdocs-autorefs==1.4.4
mkdocs-material-extensions==1.3.1

# Logging
python-json-logger==4.2.0

# Add psutil for system metrics collection
psutil==5.9.8

# The modules' own runtime dependencies (SAML/OIDC, the warehouse connectors)
# are NOT here: they live in modules/requirements.txt, locked in
# modules/requirements.lock, and are installed only by the full profile.
# Nothing under backend/ imports them.
